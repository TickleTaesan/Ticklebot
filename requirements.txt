# NoMaD Runtime Dependencies for Jetson Orin Nano (JetPack 6.2, Python 3.10)
# GPU-independent packages only
# IMPORTANT: PyTorch/torchvision and any torch-dependent libs are NOT installed here.
#            Install PyTorch first (Jetson method), then install extras from requirements-torch-extras.txt

# Core ML dependencies
numpy==1.24.3
matplotlib==3.7.1
pyyaml==6.0.1
pillow==10.0.0
tqdm==4.65.0
# On Jetson (aarch64), prefer system OpenCV (python3-opencv). Skip pip wheel on aarch64
opencv-python==4.8.0.76; platform_machine != "aarch64"
h5py==3.8.0
pyserial==3.5

# NoMaD specific (torch-agnostic here)
diffusers==0.11.1
warmup-scheduler==0.3

# Vision and attention models
einops==0.6.1
transformers==4.21.3

# Additional utilities
filelock==3.12.2
requests==2.31.0
urllib3==2.0.4
sympy==1.12
networkx==3.1

# Optional: For development and debugging
ipython==8.12.3